version: '3.8'

services:
  audible-downloader:
    build: .
    container_name: audiobookDownloader
    environment:
      - AUDIOBOOK_FOLDERS=True
    ports:
      - "5000:5000"
    volumes:
      - ./config:/config
      - ./audiobooks:/audiobooks
    restart: unless-stopped
